# cpphttp-react
A whiteapp for building a React web app with C++17 http backend.

# Building the project
```
git clone --recurse-submodules https://github.com/li-jeffrey/cpphttp-react.git
cd cpphttp-react && make
```
